import 'package:flutter/material.dart';

class bro extends StatelessWidget {
  const bro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "This is the Errors page",
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
